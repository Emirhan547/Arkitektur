
@model List<ResultFeatureDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<h3>Hizmetlerimiz</h3>

<table class="table table-bordered table-hover">
    <tr>
        <th>#</th>
        <th colspan="2">Proje Başlığı</th>
        <th>Açıklama</th>
        <th>İkon</th>
        <th>İşlemler</th>

    </tr>
    @foreach (var item in Model)
    {
        count++;
        <tr>
            <th>@count</th>
            <td colspan="2">
                <img src="@item.BackgroundImage" style="height:100px; width:100px;" />
                @item.Title
            </td>
            <td>@item.Description</td>
            <td>@item.Icon</td>

            <td>
                <button onclick="deleteFeature(@item.Id)" class="btn btn-outline-danger"><i class="fa fa-trash"></i></button>
                <a href="/Admin/Feature/UpdateFeature/@item.Id" class="btn btn-outline-info"><i class="fa fa-edit"></i></a>
            </td>
        </tr>
    }
</table>
<a asp-action="CreateFeature" asp-controller="Feature" asp-area="Admin" class="btn btn-outline-primary"><i class="fa fa-plus-circle"></i>Yeni Feature Oluştur</a>

<script>
    function deleteFeature(id){
           Swal.fire({
      title: "Silmek istediğinize emin misiniz?",
      text: "Bu işlemi geri alamazsınız",
      icon: "warning",
      showCancelButton: true,
      confirmButtonColor: "#3085d6",
      cancelButtonColor: "#d33",
      confirmButtonText: "Evet, Sil!",
      cancelButtonText: "İptal"
    }).then((result) => {
      if (result.isConfirmed) {

          window.location.href = '/Admin/Feature/DeleteFeature/' + id;

      }
    });
    }
</script>